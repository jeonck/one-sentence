# 영어 한문장 포스트 작성 가이드

## 파일 구조

```
content/posts/표현-이름.md
```

### 프론트매터

```yaml
---
title: "한국어 설명"
date: 2026-05-30T09:00:00-05:00
draft: false
tags: ["영어", "회화영어", "태그1", "태그2"]
---
```

- **파일명**: 영어 표현을 kebab-case로 (`fiddle-with.md`)
- **title**: 마지막에 '영어로' 붙이지 않음 (`"짜증 나는, 성가신"` ✓ / `"짜증 나는, 성가신 영어로"` ✗)
- **date**: 반드시 **과거 날짜**로 설정 (Hugo는 빌드 시점 기준 미래 날짜 포스트를 기본 제외)

---

## 본문 구조

```markdown
## Today's Sentence

**"영어 표현"**

> 한국어 번역

### 단어 풀이

| 단어/패턴 | 발음 | 뜻 |
|-----------|------|-----|
| 표현 | [발음] | 뜻 |

> 💡 **Tip:** 설명 내용

### 활용 예문

- 예문 1
  (한국어 번역)

- 예문 2
  (한국어 번역)

하루 한 문장으로 영어 실력을 쌓아봐요! 💪
```

---

## 볼드체(`**`) 렌더링 규칙

Hugo의 Markdown 파서(Goldmark)는 특정 패턴에서 `**볼드**`를 인식하지 못하는 문제가 있습니다. 아래 두 가지 규칙을 반드시 지켜주세요.

---

### ❌ 규칙 1: `**` 닫힘 뒤 괄호는 공백 필요

`**"표현"**(설명)` 처럼 닫는 `**` 바로 뒤에 `(`가 오면 볼드가 렌더링되지 않습니다.

| 잘못된 예 | 올바른 예 |
|-----------|-----------|
| `**"I have to pass"**(이번엔 패스해야 할 것 같아)` | `**"I have to pass"** (이번엔 패스해야 할 것 같아)` |
| `**"mandatory"**(필수적인)` | `**"mandatory"** (필수적인)` |

**규칙:** `**"표현"**` 뒤에 `(` 가 올 때는 반드시 공백을 한 칸 삽입

```markdown
✅ **"expression"** (한국어 설명)
❌ **"expression"**(한국어 설명)
```

---

### ❌ 규칙 2: `**` 닫힘 뒤 한국어 조사는 공백 필요

`**"표현"**는` 처럼 영어 표현을 닫는 따옴표(`"`) 뒤의 `**` 다음에 한국어 조사가 바로 오면 볼드가 렌더링되지 않습니다.

| 잘못된 예 | 올바른 예 |
|-----------|-----------|
| `**"See you"**는 영어에서...` | `**"See you"** 는 영어에서...` |
| `**"tired"**가 일반적인...` | `**"tired"** 가 일반적인...` |
| `**"exhausted"**는 완전히...` | `**"exhausted"** 는 완전히...` |

**규칙:** 영어 표현(`"..."`) 뒤 `**` 다음에 한국어가 올 때는 반드시 공백을 한 칸 삽입

```markdown
✅ **"expression"** 는 ...
❌ **"expression"**는 ...
```

> **주의:** 대화 예문에서 한국어를 볼드로 감쌀 때는 해당 없음
> ```markdown
> ✅ "**내일 봐요**, 다들."   ← 한국어 볼드, 규칙 적용 안 함
> ✅ **"See you"** 는          ← 영어 표현 볼드, 규칙 적용
> ```

---

## 요약 체크리스트

포스트 작성 후 Tip 섹션에서 아래 항목을 확인하세요.

- [ ] `**"표현"**(`  →  `**"표현"** (` 로 공백 추가했는가?
- [ ] `**"표현"**[한국어]`  →  `**"표현"** [한국어]` 로 공백 추가했는가?
- [ ] 포스트 날짜가 빌드 시점보다 과거인가?
- [ ] title 끝에 '영어로'가 없는가?
